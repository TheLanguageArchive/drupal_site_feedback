import { DrupalApiService } from '../api/drupal.api.service';

export class ValidatorService {

    // constructor(private drupal: DrupalApiService) {
    // }

    // oldValidate(formId) {

    //     let form = this.drupal.getForm(formId);
    //     return form.validate();
    // }

    // validate(formId) {

    //     let form = this.drupal.getForm(formId);
    //     let validations = [


    //     ];
    // }
}
